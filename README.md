# Tailscale Proxy

tsproxy is a reverse proxy that manages a [tsnet][] server for each configured
upstream. See `tsproxy --help` and `example.json` for usage.

[tsnet]: https://pkg.go.dev/tailscale.com/tsnet)
